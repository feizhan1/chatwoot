# Chatwoot 手动升级指南

## 适用环境
- Stage 环境: service.sjlpj.cn (Ubuntu)

---

## Stage 环境完整手动升级步骤

### 准备工作

```bash
# SSH 登录到 Stage 服务器
ssh user@115.175.225.110
cd /path/to/chatwoot

# 确认当前版本
docker-compose -f docker-compose.stage.yaml exec rails bundle exec rails runner 'puts Chatwoot.config[:version]'
```

### 第一步：备份数据库

```bash
# 创建备份目录
sudo mkdir -p /var/backups/chatwoot/db
sudo chown -R $USER:$USER /var/backups/chatwoot

# 备份数据库
TIMESTAMP=$(date +%Y%m%d-%H%M%S)
docker-compose -f docker-compose.stage.yaml exec -T postgres pg_dump -U postgres -d chatwoot --no-owner --no-privileges --clean | gzip > /var/backups/chatwoot/db/backup-$TIMESTAMP.sql.gz

# 验证备份（确认文件大小 > 1MB）
ls -lh /var/backups/chatwoot/db/backup-$TIMESTAMP.sql.gz
```

### 第二步：备份存储文件（可选）

```bash
VOLUME_NAME=$(docker volume ls | grep storage_data | awk '{print $2}')
sudo mkdir -p /var/backups/chatwoot/storage
docker run --rm \
  -v ${VOLUME_NAME}:/source \
  -v /var/backups/chatwoot/storage:/backup \
  alpine tar czf /backup/storage-$TIMESTAMP.tar.gz -C /source .
ls -lh /var/backups/chatwoot/storage/storage-$TIMESTAMP.tar.gz
```

### 第三步：拉取最新镜像

```bash
docker-compose -f docker-compose.stage.yaml pull
```

### 第四步：停止应用服务

```bash
# 停止应用层服务（保留数据库和Redis）
docker-compose -f docker-compose.stage.yaml stop rails sidekiq
sleep 5
```

### 第五步：启动服务并执行迁移

```bash
# 启动所有服务
docker-compose -f docker-compose.stage.yaml up -d

# 等待服务启动
sleep 30

# 执行数据库迁移（确认没有 ERROR）
docker-compose -f docker-compose.stage.yaml exec rails bundle exec rails db:migrate
```

### 第六步：验证升级

```bash
# 检查版本和服务状态
docker-compose -f docker-compose.stage.yaml exec rails bundle exec rails runner 'puts Chatwoot.config[:version]'
docker-compose -f docker-compose.stage.yaml ps

# 测试访问
curl -I https://service.sjlpj.cn
# 浏览器访问 https://service.sjlpj.cn 测试功能
```

---

## 升级回滚步骤

```bash
# 停止所有服务
docker-compose -f docker-compose.stage.yaml down

# 恢复数据库（替换为实际的备份文件名）
gunzip < /var/backups/chatwoot/db/backup-YYYYMMDD-HHMMSS.sql.gz | \
  docker-compose -f docker-compose.stage.yaml exec -T postgres \
  psql -U postgres -d chatwoot

# 恢复存储文件（如需要）
docker run --rm \
  -v chatwoot_stage_storage_data:/target \
  -v /var/backups/chatwoot/storage:/backup \
  alpine tar xzf /backup/storage-YYYYMMDD-HHMMSS.tar.gz -C /target

# 重新启动并验证
docker-compose -f docker-compose.stage.yaml up -d
curl -I https://service.sjlpj.cn
```

---

## Production 环境完整手动升级步骤

### 准备工作

```bash
ssh user@50.17.87.26
cd /var/www/chatwoot
docker-compose -f docker-compose.production.yaml exec rails bundle exec rails runner 'puts Chatwoot.config[:version]'
```

### 升级步骤

```bash
# 备份数据库
TIMESTAMP=$(date +%Y%m%d-%H%M%S)
sudo mkdir -p /var/backups/chatwoot/db
sudo chown -R $USER:$USER /var/backups/chatwoot
docker-compose -f docker-compose.production.yaml exec -T postgres \
  pg_dump -U postgres -d chatwoot --no-owner --no-privileges --clean \
  | gzip > /var/backups/chatwoot/db/backup-$TIMESTAMP.sql.gz
ls -lh /var/backups/chatwoot/db/backup-$TIMESTAMP.sql.gz

# 拉取最新镜像
docker-compose -f docker-compose.production.yaml pull

# 停止应用服务
docker-compose -f docker-compose.production.yaml stop rails sidekiq
sleep 5

# 启动服务
docker-compose -f docker-compose.production.yaml up -d
sleep 30

# 执行数据库迁移
docker-compose -f docker-compose.production.yaml exec rails bundle exec rails db:migrate

# 验证升级
docker-compose -f docker-compose.production.yaml exec rails bundle exec rails runner 'puts Chatwoot.config[:version]'
docker-compose -f docker-compose.production.yaml ps
curl -I https://cs.sjlpj.cn
# 浏览器访问 https://cs.sjlpj.cn 测试功能
```

---

## 常见问题排查

### 数据库迁移失败

```bash
docker-compose -f docker-compose.stage.yaml logs rails
docker-compose -f docker-compose.stage.yaml exec rails bash
bundle exec rails db:migrate RAILS_ENV=production
```

### 服务无法启动

```bash
docker-compose -f docker-compose.stage.yaml logs
docker-compose -f docker-compose.stage.yaml logs postgres redis rails
```

### 网站无法访问

```bash
docker-compose -f docker-compose.stage.yaml logs nginx
sudo netstat -tlnp | grep -E '(80|443|3000)'
```

### 磁盘空间不足

```bash
df -h
docker system prune -a
```

---

## 升级检查清单

**升级前：**
- [ ] 已创建数据库备份并确认文件大小正常
- [ ] 已通知用户系统维护
- [ ] 已确认磁盘空间充足

**升级后：**
- [ ] 所有容器状态正常（Up）
- [ ] 网站可以正常访问
- [ ] 核心功能正常（登录、消息收发）
- [ ] 无明显错误日志
- [ ] 版本号已更新

---

## 最佳实践

1. **升级时间**：选择业务低峰期
2. **先测试后生产**：先在 Stage 环境验证
3. **保留备份**：重要备份手动复制到其他位置
4. **监控日志**：升级后至少监控 1 小时
5. **准备回滚**：确保知道如何快速回滚

---

## 紧急处理

如果遇到严重问题：
1. 立即执行回滚步骤
2. 检查 `/var/backups/chatwoot/` 下的备份文件
3. 查看所有日志文件
