# 简单备份方案 📦

超简单的 Chatwoot 数据库备份到 AWS S3，只需 3 步！

## 🚀 快速开始

### 1. 安装依赖

```bash
# 只需安装 AWS CLI
sudo yum install -y python3-pip
pip3 install awscli

# 注意：不需要安装 PostgreSQL 客户端，使用 Docker 执行备份 ✅
```

### 2. 配置 AWS 凭证

```bash
# 设置环境变量（二选一）
export AWS_ACCESS_KEY_ID=your_access_key
export AWS_SECRET_ACCESS_KEY=your_secret_key

# 或者使用 AWS CLI 配置
aws configure
```

### 3. 修改脚本配置

编辑 `scripts/simple-backup.sh`：

```bash
# 修改这一行，设置你的 S3 存储桶名
S3_BUCKET="your-backup-bucket"  # 改成你的存储桶名
```

### 4. 运行备份

```bash
# 给脚本执行权限
chmod +x scripts/simple-backup.sh

# 执行备份
./scripts/simple-backup.sh
```

## ⏰ 设置定时备份

```bash
# 添加到 crontab（每天凌晨2点备份）
crontab -e

# 添加这一行（一行写完，包含备份和邮件通知）：
42 2 * * * cd /var/www/chatwoot && AWS_ACCESS_KEY_ID=$AWS_ACCESS_KEY_ID AWS_SECRET_ACCESS_KEY=$AWS_SECRET_ACCESS_KEY ./scripts/backup-with-check.sh >> /var/log/chatwoot_backup.log 2>&1
```

### 安装邮件发送工具

```bash
# 安装sendemail工具（用于发送邮件通知）
sudo yum install -y epel-release
sudo yum install -y sendemail

# 或者
sudo dnf install -y sendemail
```

## 📊 监控备份状态

### 检查备份是否成功

```bash
# 给监控脚本执行权限
chmod +x scripts/check-backup-status.sh

# 检查今天是否备份成功
./scripts/check-backup-status.sh

# 只检查今天的备份
./scripts/check-backup-status.sh today

# 生成详细报告
./scripts/check-backup-status.sh report
```

### 查看备份状态日志

```bash

cat /var/log/chatwoot_backup.log

# 查看备份成功/失败日志
tail -f /var/log/chatwoot-backup-status.log

# 查看最近的备份状态
tail -10 /var/log/chatwoot-backup-status.log
```

### 邮件通知功能

现在备份脚本会自动：
- ✅ 执行数据库备份
- ✅ 检查备份状态
- ✅ 发送邮件通知到 feizhan1@qq.com
- ✅ 包含备份成功/失败状态
- ✅ 显示系统资源使用情况
- ✅ 列出最新的备份文件

### 手动检查备份状态

```bash
# 检查今天的备份并发送邮件通知
./scripts/check-backup-status.sh

# 只检查今天的备份（不发邮件）
./scripts/check-backup-status.sh today

# 生成详细报告（不发邮件）
./scripts/check-backup-status.sh report
```

## 📂 备份文件结构

备份文件会保存在 S3 的这个路径：
```
s3://你的存储桶/chatwoot-backups/
├── chatwoot-backup-20240826_020000.sql.gz
├── chatwoot-backup-20240827_020000.sql.gz
└── ...
```

## 🔄 数据恢复

```bash
# 1. 从 S3 下载备份
aws s3 cp s3://your-bucket/chatwoot-backups/chatwoot-backup-20240826_020000.sql.gz /tmp/

# 2. 解压
gunzip /tmp/chatwoot-backup-20240826_020000.sql.gz

# 3. 停止 Chatwoot 服务
docker-compose -f docker-compose.production.yaml stop rails sidekiq

# 4. 恢复数据库
docker-compose -f docker-compose.production.yaml exec postgres psql -U postgres -d chatwoot < /tmp/chatwoot-backup-20240826_020000.sql

# 5. 启动服务
docker-compose -f docker-compose.production.yaml start rails sidekiq
```

## ✨ 功能特性

- ✅ **超简单配置**：只需修改存储桶名
- ✅ **无需额外依赖**：通过 Docker 执行备份，无需安装 PostgreSQL 客户端
- ✅ **自动压缩**：备份文件自动 gzip 压缩
- ✅ **自动清理**：自动删除 30 天前的备份
- ✅ **安全上传**：直接上传到 S3，本地不保留
- ✅ **错误检查**：检查 AWS 凭证和配置
- ✅ **邮件通知**：自动发送备份状态到QQ邮箱
- ✅ **一条cron任务**：备份+检查+邮件，全部自动化

## 🔧 故障排除

**Q: 提示 AWS 凭证错误？**  
A: 确保设置了 `AWS_ACCESS_KEY_ID` 和 `AWS_SECRET_ACCESS_KEY`

**Q: 提示 S3 存储桶错误？**  
A: 确保存储桶存在且有读写权限

**Q: 备份失败？**  
A: 检查数据库是否运行：`docker-compose -f docker-compose.production.yaml ps postgres`

**Q: 邮件发送失败？**  
A: 确保安装了 sendemail：`sudo yum install -y sendemail`

**Q: 脚本权限错误？**  
A: 给脚本添加执行权限：`chmod +x scripts/backup-with-check.sh`

## 📋 与复杂版本的对比

| 特性 | 简单版本 ✅ | 复杂版本 ❌ |
|------|-----------|-----------|
| 配置文件数量 | 1个脚本 | 8个脚本 + Docker配置 |
| 设置步骤 | 3步 | 10+步 |
| 系统依赖 | 只需 AWS CLI | PostgreSQL客户端 + Docker容器 |
| 维护复杂度 | 极简 | 复杂 |
| 功能完整性 | 完整 | 过度设计 |

 ## 在 crontab -e 中（通常使用 vi/vim 编辑器），移动光标到行首的快捷键：

  移动到行首：

  - 0 (数字零) - 移动到行的绝对开头
  - ^ - 移动到行的第一个非空白字符
  - Home - 在某些终端中也可以使用

  其他常用移动快捷键：

  - $ - 移动到行尾
  - w - 移动到下一个单词开头
  - b - 移动到上一个单词开头
  - gg - 移动到文件开头
  - G - 移动到文件结尾

  编辑模式：

  - i - 在当前位置插入
  - I - 在行首插入
  - A - 在行尾插入
  - o - 在下方新建一行并插入

  最常用的是 0 键，可以快速移动到行的最开始位置。

选择简单版本，让备份变得轻松！🎉